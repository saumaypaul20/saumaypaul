/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: "/saumaypaul",
    output: "export",
    reactStrictMode: true,
    images:{
        path:"/saumaypaul"
    }
};

export default nextConfig;
